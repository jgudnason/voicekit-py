# Test fixtures

Tiny synthetic audio files used by the unit tests in `tests/`. Everything
here is committed to git, so keep files small (a few seconds of audio at
most) and synthetic — no corpus material, no recordings of identifiable
speakers.
